# vendored wayland-scanner 0.31.10

This is the published `wayland-scanner` 0.31.10 source with the upstream
`quick-xml` 0.41 parser API update applied. It remains vendored only until a
compatible upstream release includes the security fix without the unreleased
Wayland client trait changes.

The source is MIT licensed; see `LICENSE.txt`.
